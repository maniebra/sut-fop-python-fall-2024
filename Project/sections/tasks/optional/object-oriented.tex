در این بخش شما باید مدل‌هایتان را به جای استفاده از لغت‌نامه، با استفاده از رویکرد شی‌گرا ایجاد کنید. سه کلاس \code{Student}, \code{Professor} و \code{Course} را ایجاد کرده و توابع مورد نیاز برای این کلاس‌ها را پیاده‌سازی کنید.

\subsection{ساختار کلاس \lr{Student}}

ساختار کد شما برای کلاس \lr{Student} می‌تواند به شکل زیر باشد:

\sourcecode{codes/3.1.1.py}

\subsection{ساختار کلاس \lr{Professor}}

ساختار کد شما برای کلاس \lr{Professor} می‌تواند به شکل زیر باشد:

\sourcecode{codes/3.1.2.py}

\subsection{ساختار کلاس \lr{Course}}

ساختار کد شما برای کلاس \lr{Course} می‌تواند به شکل زیر باشد:

\sourcecode{codes/3.1.3.py}

توجه شود که در صورت پیاده‌سازی صحیح کلاس‌ها با ساختار شی‌گرا، نیازی به پیاده‌سازی آن‌ها با لغت‌نامه \underline{نیست} و نمره‌ی آن بخش نیز به صورت کامل به شما تعلق خواهد گرفت.